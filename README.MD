# Duniter server image

## Usage

Store your keys in a file named keyfile.

`docker run -v <path to keyfile dir>:/keys nainformatique/duniter`

You can also provide the `/keys/keyfile` file using a configmap or similar tools.
